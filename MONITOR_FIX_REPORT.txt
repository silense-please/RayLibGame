[x >= monitor_x && x <'=' (monitor_x + width)] && [y >= monitor_y && y <'=' (monitor_y + height)]

bug occurs when maximizing the window on second monitor (the monitor on the right) 
GetCurrentMonitor() returns the primary monitor



bug fix in GetCurrentMonitor() - due to monitor coordinates (mx/my) starts from zero (следовательно)-> 
'x <= (mx + width)' is already a coordinate of the next monitor
this results into detecting wrong monitor when window is maximized(on secoundary monitor which is
on the right or at the bottom of the primary monitor)

commit - fixed GetCurrentMonitor() wrong monitor detection then window is maximized on secondary monitor


optionally screen coordinates could be moved from the top left corner of the window to the center
to ensure Windows getting proper monitor 
because GetCurrentMonitor() still can return wrong monitor in rare cases (for example when you make window
borderless fullscreen and you have taskbar on the left or on top).
But I don't know if that breaks something else on other platforms, though it shouldn't.

или опционально можно переместить точку определения экрана из угла в центр окна, это тоже чинит проблему
если это не поломает другой функционал